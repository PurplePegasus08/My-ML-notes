Machine learning only started to flourish in the 1990s, it has quickly become the most popular and most successful subfield of AI, a trend driven by theavailability of faster hardware and larger datasets. Machine learning is tightly related to mathematical statistics, but it differs from statistics in several important ways.Unlike statistics, machine learning tends to deal with large, complex datasets (such asa dataset of millions of images, each consisting of tens of thousands of pixels) forwhich classical statistical analysis such as Bayesian analysis would be impractical.Machine-learning algorithms aren’t usually creative in they’re merely searching through a predefined set of operations, called a hypothesis space.


Deep Learning (DL) - Deep learning is a specific subfield of machine learning: a new take on learning repre-sentations from data that puts an emphasis on learning successive layers of increasingly meaningful representations.How many layers contribute to a model of the data is called the depth of the model. Other appropriate names for the field could have been layered representations learning and hierarchical representations learning and they’re all learned automatically from exposure to training data.


Meanwhile,
other approaches to machine learning tend to focus on learning only one or two lay- ers of representations of the data; hence, they’re sometimes called shallow learning.

In deep learning, these layered representations are (almost always) learned via models called neural networks, structured in literal layers stacked on top of each other.

Understanding how deep learning works, in three figures

The specification of what a layer does to its input data is stored in the layer’s weights, which in essence are a bunch of numbers. In technical terms, we’d say that the transformation implemented by a layer is parameterized by its weights (Weights are also sometimes called the parameters of a layer.). In this context, learning means finding a set of values for the weights of all layers in a network, such that the network will correctly map example inputs to their associated targets.

 Loss function : To control the output of a neural network, you need to be able to measure how far this output is from what you expected. This is the job of the loss function of the network, also called the objective function. The loss function takes the predictions of the network and the true target (what you wanted the network to output) and computes a distance score, capturing how well the network has done on this specific example. The fundamental trick in deep learning is to use this score as a feedback signal to adjust the value of the weights a little, in a direction that will lower the loss score for the current example  . This adjustment is the job of the optimizer, which implements what’s called the Backpropagation algorithm: the central algorithm in deep learning.


 Initially, the weights of the network are assigned random values, so the network merely implements a series of random transformations. Naturally, its output is far from what it should ideally be, and the loss score is accordingly very high. But with every example the network processes, the weights are adjusted a little in the correct direction, and the loss score decreases. This is the training loop, which, repeated a suffi-cient number of times (typically tens of iterations over thousands of examples), yields weight values that minimize the loss function.



=============================================================================================

Supervised Classification Algorithms
Classification algorithms are used when the output variable is a categorical variable, meaning it falls into discrete classes or categories. The goal is to predict which category new, unseen data belongs to.


Examples of classification problems include:

Spam detection (spam/not spam)
Image recognition (cat/dog/bird)
Disease diagnosis (diseased/not diseased)
Sentiment analysis (positive/negative/neutral)
Common supervised classification algorithms include:

Logistic Regression: Despite its name, Logistic Regression is a classification algorithm. It's often used for binary classification problems (two classes) but can be extended to multi-class classification. It estimates the probability of an instance belonging to a particular class.


Support Vector Machines (SVM): SVMs work by finding the optimal hyperplane that separates data points into different classes with the largest margin possible. They can be used for both linear and non-linear classification problems (using kernels).

Decision Trees: These algorithms classify data by creating a tree-like structure of decisions based on features. Each internal node represents a test on an attribute, each branch represents an outcome of the test, and each leaf node represents a class label.

Random Forests: An ensemble learning method that builds multiple decision trees during training and outputs the class that is the mode of the classes (classification) or mean prediction (regression) of the individual trees. This helps reduce overfitting and improve accuracy compared to a single decision tree.

K-Nearest Neighbors (KNN): A non-parametric, "lazy" learning algorithm that classifies new data points based on the majority class of their 'k' nearest neighbors in the feature space.
Naive Bayes: A family of algorithms based on Bayes' Theorem with the "naive" assumption of independence between features. They are particularly popular for text classification and spam filtering.
Perceptron: One of the simplest and oldest algorithms, primarily used for binary classification. It's a foundational algorithm for neural networks.
Supervised Continuous (Regression) Algorithms
Regression algorithms are used when the output variable is a continuous numerical value, meaning it can take on any value within a range. The goal is to predict a numerical outcome.

Examples of continuous prediction (regression) problems include:

Predicting house prices
Forecasting stock prices
Predicting temperature
Estimating a person's age
Predicting sales figures
Common supervised continuous (regression) algorithms include:

Linear Regression: A simple and widely used algorithm that models the linear relationship between a dependent variable and one or more independent variables by fitting a straight line to the data. 2. Polynomial Regression: An extension of linear regression that models the relationship between the independent variable and the dependent variable as an nth-degree polynomial.

Lasso Regression & Ridge Regression: These are regularization techniques applied to linear regression to prevent overfitting by adding penalty terms to the cost function.
Support Vector Regression (SVR): An extension of Support Vector Machines (SVMs) used for regression tasks. Instead of finding a hyperplane that separates classes, SVR finds a hyperplane that best fits the continuous data, minimizing the error within a certain margin.
Algorithms Used in Both Problems
Several algorithms are versatile enough to be used for both classification and continuous (regression) problems, primarily by adapting their output layer or objective function:

Decision Trees: As mentioned above, decision trees can be used for both classification (predicting a category) and regression (predicting a continuous value). For regression, the leaf nodes typically represent the average or median of the target variable for the data points that fall into that leaf.

Random Forests: Since Random Forests are an ensemble of decision trees, they also inherit the ability to handle both classification and regression tasks. For regression, they average the predictions of individual trees.

K-Nearest Neighbors (KNN): For classification, KNN predicts the class based on the majority vote of neighbors. For regression, it predicts the continuous value by averaging the values of its 'k' nearest neighbors.

Support Vector Machines (SVM) / Support Vector Regression (SVR): While SVM is primarily known for classification, its extension, SVR, is specifically designed for continuous prediction problems. The underlying principle of finding an optimal hyperplane is adapted for regression by fitting a function to the data rather than separating it.

Neural Networks (Deep Learning): Neural networks are highly flexible and can be configured for both classification and regression. The distinction often lies in the activation function of the output layer and the loss function used during training. 
For Classification: The output layer often uses a softmax activation function (for multi-class) or sigmoid (for binary), and a cross-entropy loss function.
For Regression: The output layer usually has a linear activation function, and a mean squared error (MSE) or mean absolute error (MAE) loss function.
These algorithms demonstrate the adaptability of many machine learning techniques, allowing them to solve a wide range of predictive modeling problems by adjusting their internal mechanisms for discrete or continuous outputs.



------------------------------------------------------------------------


Supervised Classification Algorithms (Corrected and Expanded)
Logistic Regression: ...
Support Vector Machines (SVM): ...
Decision Trees: ...
Random Forests: ...
K-Nearest Neighbors (KNN): ...
Naive Bayes: A family of algorithms based on Bayes' Theorem with the "naive" assumption of independence between features. They are particularly popular for text classification, spam filtering, and recommendation systems due to their simplicity and efficiency.
Perceptron: ...
Stochastic Gradient Descent (SGD) Classifiers: While SGD is an optimization algorithm, it's often used to train linear classifiers (like Logistic Regression and SVMs) very efficiently, especially on large datasets. So, you might see "SGDClassifier" in libraries which essentially implements a linear classifier trained with SGD.
Supervised Continuous (Regression) Algorithms (No major omissions, but worth noting a few more specific variants)
Linear Regression: ...
Polynomial Regression: ...
Lasso Regression & Ridge Regression: ...
Elastic Net Regression: A regularization technique that combines both Lasso and Ridge penalties.
Support Vector Regression (SVR): ...
Gradient Boosting Regressors (e.g., Gradient Boosting Machines - GBM, XGBoost, LightGBM, CatBoost): These are powerful ensemble techniques that build additive models in a forward stagewise manner, often yielding highly accurate predictions. While the general "Gradient Boosting" applies to both, specific implementations like XGBoost are very popular for regression.
Algorithms Used in Both Problems (No major omissions, but good to reiterate their versatility)
Decision Trees: ...
Random Forests: ...
K-Nearest Neighbors (KNN): ...
Support Vector Machines (SVM) / Support Vector Regression (SVR): ...
Neural Networks (Deep Learning): ...
Gradient Boosting Machines (GBM): As mentioned, while often highlighted for regression, GBMs (and their optimized variants like XGBoost, LightGBM) are equally powerful and widely used for classification tasks. The objective function is simply changed (e.g., to log-loss for classification).
